spring:

# in-memory mode is default

#  datasource:
#    url: jdbc:h2:mem:test # in-memory mode. Only test version.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # drop if exists and making entity
#    properties:
#      hibernate:
#        #show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace